# Next.js
.next/
out/

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.production

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Build
dist/
build/

# Node.js localStorage (legacy, no longer used)
data/.localstorage
data/.localstorage-*

# Uploaded binary files (files are now stored as Base64 in vault_db.json)
# Keep .gitkeep so the uploads folder exists, but ignore any actual files
data/uploads/*
!data/uploads/.gitkeep

# vault_db.json MUST be committed so Railway has an initial DB on first deploy
# (Do NOT add data/vault_db.json to this file)

# Auto-generated
next-env.d.ts

# Old project docs
Project-proposal.md

tsconfig.tsbuildinfo
